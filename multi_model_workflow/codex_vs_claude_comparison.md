# Codex vs Claude Comparison

| Category | Codex | Claude | Final Choice |
| --- | --- | --- | --- |
| Theorem statement | Matched target shape after repair | Matched target shape in all three attempts | Preserve project theorem statement |
| Assumptions | Used uniform convergence and ERM | Used uniform convergence and ERM; kept directions explicit via `.left` / `.right` | Keep assumptions explicit |
| Proof style | `calc` proof with named intermediate inequalities | `calc` proof; simplified version inlines hypotheses directly | Use readable `calc` proof |
| Errors | Treated `hUC hHat` as one inequality; used ERM without `+ eps`; arithmetic gap | Blind attempt used `omega` which cannot reason over abstract `Nat`-valued functions | Extract UC directions and use `Nat.add_le_add_right` for monotonicity |
| Useful ideas | Correct high-level proof chain | Correct high-level chain; identified explicit Nat lemmas needed | Keep explicit lemma calls for readability |
| Final contribution | Repaired proof and simplified direct-call version | Repaired proof (attempt 2) and simplified single-`calc` version | Both models converged on the same four-step calc chain |

## Verification Results

| File | Compiles? |
| --- | --- |
| `generated_proofs/claude/agnostic_attempt_1.lean` | No — `omega` failure on abstract Nat functions |
| `generated_proofs/claude/agnostic_attempt_2_repaired.lean` | Yes |
| `generated_proofs/claude/simplified_attempt.lean` | Yes |

## Key Difference

Codex's blind error was a **semantic** gap (wrong inequality direction).
Claude's blind error was a **tactic** gap (`omega` vs. explicit Nat lemmas).
Both were repaired with the same logical structure.

## Script-Backed Comparison

The repeatable comparison is generated by:

```powershell
python multi_model_workflow/run_comparison.py
```

Output:

```text
multi_model_workflow/comparison_report.md
```

This Python script performs the verifier checks and extracts basic structural features. This file is the interpretation layer; the generated report is the evidence layer.
